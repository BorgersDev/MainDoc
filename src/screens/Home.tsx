import { HomeHeader } from "@components/HomeHeader";
import { Center, Text, VStack } from "@gluestack-ui/themed";




export const Home = ( ) => {
    return (
        <VStack flex={1} >
            <HomeHeader />
        </VStack>
    )
}